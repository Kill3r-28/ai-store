# Copy to secrets.toml and fill in. Never commit secrets.toml.
# Streamlit Cloud: paste into Settings -> Secrets.

# Optional. Read-only GitHub token used to fetch README and docs/future_plans.md
# from registered tools' repos. Without it, the app falls back to the inline
# README and roadmap entered at registration.
GITHUB_TOKEN = "ghp_your_token_here"
